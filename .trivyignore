# Trivy ignore file for development/build-time only vulnerabilities
# These packages exist only in builder stages and are not in final production images

# npm internal build tools (not in production image)
CVE-2025-5889   # brace-expansion - build tool
CVE-2024-21538  # cross-spawn - build tool  
CVE-2026-24001  # diff - build tool
CVE-2025-64756  # glob - build tool
CVE-2026-26996  # minimatch - build tool (multiple CVEs)
CVE-2026-27903  # minimatch - build tool
CVE-2026-27904  # minimatch - build tool
CVE-2026-23745  # tar - build tool (multiple CVEs)
CVE-2026-23950  # tar - build tool
CVE-2026-24842  # tar - build tool
CVE-2026-26960  # tar - build tool

# Note: These are npm's internal dependencies used during build/install
# They are removed before final image creation and never reach production.
# Verification:
# - API uses multi-stage Docker build (stages 1 & 3 = production)
# - Builder stage (stage 2) is discarded, not included in final image
# - npm ci --only=production in stage 1 excludes dev dependencies
