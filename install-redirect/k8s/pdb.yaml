apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: install-redirect
  namespace: iotistic-utils
spec:
  minAvailable: 2  # Always keep at least 2 pods running
  selector:
    matchLabels:
      app: install-redirect
