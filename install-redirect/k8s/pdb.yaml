apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: install-redirect
  namespace: iotistica-utils
spec:
  minAvailable: 1  # Always keep at least 1 pod running
  selector:
    matchLabels:
      app: install-redirect
