apiVersion: postgresql.cnpg.io/v1
kind: ImageCatalog
metadata:
  name: timescaledb
  namespace: iotistic-system
spec:
  images:
  - major: 17
    # TimescaleDB image on Docker Hub (CNPG requires concrete version tags like :17.2)
    image: iotistic/timescaledb-cnpg:17.2
