# Auto-generated docker-compose file for 1 agents
# Generated: 2026-03-24 08:17:08
# Agents: 1 to 1
# Ports: 48481 to 48481

services:
  agent-1:
    container_name: agent-1
    build:
      context: .
      dockerfile: agent/Dockerfile
    restart: always
    mem_limit: 512m
    mem_reservation: 256m
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    devices:
      - /dev/net/tun:/dev/net/tun
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - agent-1-data:/app/data
      - ./certs/ca.crt:/app/certs/ca.crt:ro
    environment:
      - DEVICE_API_PORT=48481
      - IOTISTICA_API=https://localhost:3443
      - NODE_ENV=development
      - MQTT_BROKER_URL=mqtt://localhost:5884
      - MQTT_USERNAME=admin
      - MQTT_PASSWORD=iotistic42!
      # Bootstrap & Security (not dashboard-controlled)
      - REQUIRE_PROVISIONING=false
      - PROVISIONING_KEY=faf9e08fe01361e47a8ecaeb748d51185c1d51eb1b673f909b66dbba8c00d877
      - API_SECURITY_MODE=LOCALHOST_ONLY
      - FIREWALL_ENABLED=false
      # Testing & Development (not dashboard-controlled)
      - SIMULATION_MODE=true
      - SIMULATION_PROFILE=baseline-realistic
      - SIMULATION_CONFIG='{"scenarios":{"anomaly_injection":{"metrics":["cpu_temp","memory_percent"],"intervalMs":60000,"severity":"warning","magnitude":2,"pattern":"spike","enabled":true},"sensor_data":{"enabled":true,"publishIntervalMs":10000,"devices":[{"unit":"C","variance":2.0,"max":35,"protocol":"modbus","baseValue":23.0,"metric":"temperature","min":15,"endpointTopic":"modbus"},{"unit":"%","variance":10.0,"max":80,"protocol":"snmp","baseValue":55.0,"metric":"humidity","min":30,"endpointTopic":"snmp"},{"unit":"hPa","variance":5.0,"max":1050,"protocol":"opcua","baseValue":1013.25,"metric":"pressure","min":980,"endpointTopic":"opcua"}],"pattern":"realistic"}}}'
      - SIMULATE_MEMORY_LEAK=false
      - USE_MSGPACK_POC=false
      - USE_KEY_COMPACTION_POC=false
      - USE_DEFLATE_COMPRESSION=false
      - ENABLE_HEAP_PROFILING=true
      - PIPELINE_FLOWS_FILE=/app/data/flows/opcua-transform.flows.json
      # Shell security (HMAC signature verification)
      - AGENT_SHELL_HMAC_KEY=wcw3eFT/zyVZ7HYwP+bApLjJX2gW/e1mHW2+WPG2gBk=
      - AGENT_SHELL_MAX_SESSION_MS=3600000
volumes:
  agent-1-data:
    driver: local
networks:
  iotistic-net:
    driver: bridge
