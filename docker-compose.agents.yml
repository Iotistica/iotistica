# Auto-generated docker-compose file for 1 agents
# Generated: 2026-03-24 08:25:26
# Agents: 1 to 1
# Ports: 48481 to 48481

services:
  agent-1:
    container_name: agent-1
    build:
      context: .
      dockerfile: agent/Dockerfile
    restart: always
    mem_limit: 512m
    mem_reservation: 256m
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    devices:
      - /dev/net/tun:/dev/net/tun
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - agent-1-data:/app/data
      - ./certs/ca.crt:/app/certs/ca.crt:ro
    environment:
      - DEVICE_API_PORT=48481
      - IOTISTICA_API=https://localhost:3443
      - NODE_ENV=development
      - MQTT_BROKER_URL=mqtt://localhost:5884
      - MQTT_USERNAME=admin
      - MQTT_PASSWORD=iotistic42!
      # Bootstrap & Security (not dashboard-controlled)
      - REQUIRE_PROVISIONING=false
      - PROVISIONING_KEY=bd83971860d60e7cffd9d7d4a1e76fe9bf8fcebd997cde79d5032ae4f05fb3d9
      - API_SECURITY_MODE=LOCALHOST_ONLY
      - FIREWALL_ENABLED=false
      # Testing & Development (not dashboard-controlled)
      - SIMULATION_MODE=true
      - SIMULATION_PROFILE=baseline-realistic
      - SIMULATION_CONFIG='{"scenarios":{"sensor_data":{"devices":[{"endpointTopic":"modbus","min":15,"metric":"temperature","baseValue":23.0,"max":35,"unit":"C","variance":2.0,"protocol":"modbus"},{"endpointTopic":"snmp","min":30,"metric":"humidity","baseValue":55.0,"max":80,"unit":"%","variance":10.0,"protocol":"snmp"},{"endpointTopic":"opcua","min":980,"metric":"pressure","baseValue":1013.25,"max":1050,"unit":"hPa","variance":5.0,"protocol":"opcua"}],"enabled":true,"pattern":"realistic","publishIntervalMs":10000},"anomaly_injection":{"severity":"warning","enabled":true,"metrics":["cpu_temp","memory_percent"],"intervalMs":60000,"magnitude":2,"pattern":"spike"}}}'
      - SIMULATE_MEMORY_LEAK=false
      - USE_MSGPACK_POC=false
      - USE_KEY_COMPACTION_POC=false
      - USE_DEFLATE_COMPRESSION=false
      - ENABLE_HEAP_PROFILING=true
      - PIPELINE_FLOWS_FILE=/app/data/flows/opcua-transform.flows.json
      # Shell security (HMAC signature verification)
      - AGENT_SHELL_HMAC_KEY=wcw3eFT/zyVZ7HYwP+bApLjJX2gW/e1mHW2+WPG2gBk=
      - AGENT_SHELL_MAX_SESSION_MS=3600000
volumes:
  agent-1-data:
    driver: local
networks:
  iotistic-net:
    driver: bridge
