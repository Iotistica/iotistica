# Auto-generated docker-compose file for 1 agents
# Generated: 2025-12-12 14:06:20
# Agents: 27 to 27
# Ports: 48507 to 48507

services:
  agent-27:
    container_name: agent-27
    image: iotistic/agent:latest
    restart: always
    mem_limit: 512m
    mem_reservation: 256m
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - agent-27-data:/app/data
      - ./certs/ca.crt:/app/certs/ca.crt:ro
    environment:
      - DEVICE_API_PORT=48507
      - CLOUD_API_ENDPOINT=http://api:3002
      - NODE_ENV=development
      # Bootstrap & Security (not dashboard-controlled)
      - REQUIRE_PROVISIONING=false
      - PROVISIONING_KEY=7ace06e952368d28a3bd43b0f51d87af28cb0a7e603553cb89584c9715aced7d
      - API_SECURITY_MODE=LOCALHOST_ONLY
      - FIREWALL_ENABLED=false
      # Testing & Development (not dashboard-controlled)
      - SIMULATION_MODE=false
      - SIMULATION_CONFIG={}
      - SIMULATE_MEMORY_LEAK=false
    networks:
      - iotistic-net
volumes:
  agent-27-data:
    driver: local
networks:
  iotistic-net:
    driver: bridge
