# Auto-generated docker-compose file for 1 agents
# Generated: 2026-02-18 19:35:18
# Agents: 2 to 2
# Ports: 48482 to 48482

services:
  agent-2:
    container_name: agent-2
    build:
      context: .
      dockerfile: agent/Dockerfile
    restart: always
    mem_limit: 512m
    mem_reservation: 256m
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    devices:
      - /dev/net/tun:/dev/net/tun
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - agent-2-data:/app/data
      - ./certs/ca.crt:/app/certs/ca.crt:ro
    environment:
      - DEVICE_API_PORT=48482
      - CLOUD_API_ENDPOINT=https://localhost:3443
      - NODE_ENV=development
      # Bootstrap & Security (not dashboard-controlled)
      - REQUIRE_PROVISIONING=false
      - PROVISIONING_KEY=915f05685997f5eb6dbaf6263fe93e4dc64cc1cdd70992c937f85bb494e04d28
      - API_SECURITY_MODE=LOCALHOST_ONLY
      - FIREWALL_ENABLED=false
      # Testing & Development (not dashboard-controlled)
      - SIMULATION_MODE=false
      - SIMULATION_CONFIG={}
      - SIMULATE_MEMORY_LEAK=false
      - USE_MSGPACK_POC=true
      - USE_KEY_COMPACTION_POC=false
      - USE_DEFLATE_COMPRESSION=true
      - ENABLE_HEAP_PROFILING=true
volumes:
  agent-2-data:
    driver: local
networks:
  iotistic-net:
    driver: bridge
