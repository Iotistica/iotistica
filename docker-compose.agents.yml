# Auto-generated docker-compose file for 1 agents
# Generated: 2026-01-08 23:30:42
# Agents: 27 to 27
# Ports: 48507 to 48507

services:
  agent-27:
    container_name: agent-27
    build:
      context: .
      dockerfile: agent/Dockerfile
    restart: always
    mem_limit: 512m
    mem_reservation: 256m
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    devices:
      - /dev/net/tun:/dev/net/tun
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - agent-27-data:/app/data
      - ./certs/ca.crt:/app/certs/ca.crt:ro
    environment:
      - DEVICE_API_PORT=48507
      - CLOUD_API_ENDPOINT=https://localhost:3443
      - NODE_ENV=development
      # Bootstrap & Security (not dashboard-controlled)
      - REQUIRE_PROVISIONING=false
      - PROVISIONING_KEY=c07afb0de5b41b9c7e7e75a859d4c13696c291d8ed6355b4afda6d9721698f63
      - API_SECURITY_MODE=LOCALHOST_ONLY
      - FIREWALL_ENABLED=false
      # Testing & Development (not dashboard-controlled)
      - SIMULATION_MODE=false
      - SIMULATION_CONFIG={}
      - SIMULATE_MEMORY_LEAK=false
      - USE_MSGPACK_POC=true
      - USE_KEY_COMPACTION_POC=false
      - USE_DEFLATE_COMPRESSION=true
      - ENABLE_HEAP_PROFILING=true
volumes:
  agent-27-data:
    driver: local
networks:
  iotistic-net:
    driver: bridge
